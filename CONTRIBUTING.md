# Contributing

1. Use Python 3.11.
2. Create a feature branch.
3. Run `ruff check .` and `pytest`.
4. Preserve SEC provenance on every new evidence path.
5. Do not add paid APIs as mandatory dependencies.
6. Do not commit downloaded filing corpora, model weights, secrets, or fabricated metrics.
7. New graph relationships require evidence spans and temporal validity fields.
