# Optional Next.js frontend

The complete core recruiter demo is Gradio. A Next.js frontend is intentionally optional so the project does not stack frameworks without measurable value. A future Vercel case-study frontend can consume the FastAPI endpoints after the local system and evaluation are frozen.
